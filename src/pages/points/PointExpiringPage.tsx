import { useMemo,useState } from 'react';
import { DataGrid } from '../../components/DataGrid';
import type { Cell,GridColumn,GridRow } from '../../components/DataGrid/types';
import { ExcelDownloadButton } from '../../components/common/ExcelDownloadButton';
import { DatePicker } from '../../components/forms/DatePicker';
import { MEMBER_BALANCES,fmtPoint } from './pointsData';
import styles from './pointSubpages.module.css';
type ExpiringRow={id:string;member:string;expiresAt:string;amount:number;available:number;status:string};
const ENTRIES:ExpiringRow[]=MEMBER_BALANCES.flatMap(b=>b.expiringBatches.map((batch,index)=>({id:`EXP-${b.member}-${index+1}`,member:b.member,expiresAt:batch.expiresAt,amount:batch.amount,available:b.available,status:b.memberStatus})));
const COLUMNS:GridColumn[]=[{label:'소멸 예정 번호'},{label:'소멸 예정일'},{label:'회원'},{label:'회원 상태'},{label:'소멸 예정 포인트',align:'right'},{label:'사용 가능 포인트',align:'right'},{label:'잔여일',align:'right'}];
export function PointExpiringPage(){const [keyword,setKeyword]=useState('');const [search,setSearch]=useState('');const [status,setStatus]=useState('');const [start,setStart]=useState('');const [end,setEnd]=useState('');const rows=useMemo<GridRow[]>(()=>ENTRIES.filter(e=>!search||`${e.id} ${e.member}`.toLowerCase().includes(search.toLowerCase())).filter(e=>!status||e.status===status).filter(e=>(!start||e.expiresAt>=start)&&(!end||e.expiresAt<=end)).map(e=>{const days=Math.ceil((new Date(`${e.expiresAt}T00:00:00`).getTime()-new Date('2026-08-26T00:00:00').getTime())/86400000);return{id:e.id,cells:[{kind:'text',text:e.id,weight:650,numeric:true},{kind:'text',text:e.expiresAt,weight:650,numeric:true},{kind:'text',text:e.member,weight:650},{kind:'badge',text:e.status,bg:e.status==='정상'?'#ecfdf5':'#f4f4f5',fg:e.status==='정상'?'#047857':'#71717a'},{kind:'text',text:fmtPoint(e.amount),color:'#c2410c',weight:700,align:'right',numeric:true},{kind:'text',text:fmtPoint(e.available),align:'right',numeric:true},{kind:'text',text:`D-${days}`,color:days<=7?'#dc2626':'#71717a',weight:650,align:'right',numeric:true}] as Cell[]}}),[search,status,start,end]);const reset=()=>{setKeyword('');setSearch('');setStatus('');setStart('');setEnd('')};return <div className={styles.page}><header className={styles.header}><div className={styles.title}>소멸 예정</div><div className={styles.subtitle}>유효기간이 임박한 회원별 포인트와 소멸 예정일을 조회합니다.</div></header><section className={styles.summary}><div className={styles.summaryCard}><span>소멸 예정 회원</span><strong>{new Set(ENTRIES.map(e=>e.member)).size}명</strong></div><div className={styles.summaryCard}><span>소멸 예정 건수</span><strong>{ENTRIES.length}건</strong></div><div className={styles.summaryCard}><span>소멸 예정 포인트</span><strong>{fmtPoint(ENTRIES.reduce((s,e)=>s+e.amount,0))}</strong></div></section><section className={styles.filterBox}><form className={styles.filterRow} onSubmit={e=>{e.preventDefault();setSearch(keyword.trim())}}><label className={styles.field}><input value={keyword} onChange={e=>setKeyword(e.target.value)} placeholder="소멸 예정 번호 / 회원"/></label><label className={styles.field}><span>회원 상태</span><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">전체 회원 상태</option><option>정상</option><option>휴면</option><option>탈퇴</option></select></label><label className={styles.dateField}><span>소멸 예정일</span><div className={styles.dateRange}><DatePicker value={start} onChange={e=>setStart(e.target.value)}/><span>~</span><DatePicker value={end} onChange={e=>setEnd(e.target.value)}/></div></label><button className={styles.searchButton}>조회</button><button type="button" className={styles.resetButton} onClick={reset}>초기화</button></form></section><div className={styles.resultRow}><strong>총 {rows.length}건</strong><div className={styles.resultActions}><ExcelDownloadButton type="button" data-grid-download/><select className={styles.pageSizeSelect} defaultValue="20개씩 보기"><option>20개씩 보기</option><option>50개씩 보기</option></select></div></div><div className={styles.grid}><DataGrid columns={COLUMNS} rows={rows} gridTemplate="170px 130px 1fr 100px 140px 140px 90px" minWidth="950px" empty={rows.length===0} emptyText="검색 조건에 해당하는 소멸 예정 내역이 없습니다."/></div></div>}
